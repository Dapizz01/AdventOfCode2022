For this calendar challenge i changed the input format into another.
I changed the stack initial setup format because it was hard to parse, and i also changed the instruction format (although it wasn't necessary)
